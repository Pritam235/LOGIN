import React from "react";

const Dashboard = () => {
  return (
    <div
      style={{
        minHeight: "100vh",
        display: "flex",
        justifyContent: "center",
        alignItems: "center",
        fontSize: "40px",
      }}
    >
      Welcome to Dashboard 🎉
    </div>
  );
};

export default Dashboard;